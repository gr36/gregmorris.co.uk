{{#get "posts" limit="3" filter="tags:[{{post.tags}}]+tags:-[link,micro]+id:-{{post.id}}" as |related|}}
    {{#if related}}
        <section class="related-posts">
            <div class="related-container">
                <h3 class="related-title">You Might Also Like...</h3>
                <div class="post-feed">
                    {{#foreach posts}}
                        <div class="grid-item grid-sizer"></div>
                        {{> "blog_loop_related"}}
                    {{/foreach}}
                </div>
            </div>
        </section>
    {{/if}}
{{/get}}