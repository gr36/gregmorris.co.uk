<div class="podcast-card">
            <div class="podcast-card-image">
                <a class="podcast-card-image-wrap" href="{{url}}" style="background-image:url({{img_url feature_image size="s"}})"></a>
            </div>

                <div class="post-card-info">
                <h2 class="podcast-title"><a href="{{url}}">{{title}}</h2></a>
                <div class="podcast-meta">
                    <time datetime="{{date format="YYYY-MM-DD"}}">Posted on {{date published_at}}</time>
                </div>
            </div>

        </div>